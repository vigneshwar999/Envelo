diff --git a/dist/types/icns.js b/dist/types/icns.js
index f2bfafef3723cb423b110304815e56e3f97f81e0..82f8ef53694f234ad85023f2decb104205aa15f2 100644
--- a/dist/types/icns.js
+++ b/dist/types/icns.js
@@ -65,9 +65,16 @@ const ICON_TYPE_SIZE = {
 };
 function readImageHeader(input, imageOffset) {
     const imageLengthOffset = imageOffset + ENTRY_LENGTH_OFFSET;
+    const imageLength = (0, utils_1.readUInt32BE)(input, imageLengthOffset);
+    // Security guard (CVE-2025-71330 / GHSA-w3rx-r6r6-pgpr): a zero or
+    // undersized entry length never advances the offset, causing an
+    // infinite loop. Every valid entry is at least 8 bytes (type + length).
+    if (imageLength < SIZE_HEADER) {
+        throw new TypeError('Invalid ICNS, entry length must be at least 8');
+    }
     return [
         (0, utils_1.toUTF8String)(input, imageOffset, imageLengthOffset),
-        (0, utils_1.readUInt32BE)(input, imageLengthOffset),
+        imageLength,
     ];
 }
 function getImageSize(type) {
diff --git a/dist/types/jxl.js b/dist/types/jxl.js
index f557b0aea7e679d2cb84d9915e957dd2254eba97..8fe56d76384ed5cc9673b887828b3d63b83e54c7 100644
--- a/dist/types/jxl.js
+++ b/dist/types/jxl.js
@@ -24,7 +24,10 @@ function extractPartialStreams(input) {
         if (!jxlpBox)
             break;
         partialStreams.push(input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size));
-        offset = jxlpBox.offset + jxlpBox.size;
+        // Security guard (CVE-2025-71329 / GHSA-5p2g-fcmc-qvqq): a zero-size
+        // jxlp box would keep the offset stuck on the same box forever,
+        // because findBox returns the match before its own advance guard.
+        offset = jxlpBox.offset + Math.max(jxlpBox.size, 8);
     }
     return partialStreams;
 }
